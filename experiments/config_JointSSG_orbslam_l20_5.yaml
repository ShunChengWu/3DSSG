inherit_from: [experiments/config_base_3RScan_orbslam_l20.yaml, experiments/config_base_JointSSG.yaml]
name: 'JointSSG_orbslam_l20_5' # 
training:
 batch: 1
 data_workers: 12
 validate_every: 100 # ignore validation in dev
model:
 use_rgb: false # color is also bad
 use_normal: false # no normal for orbslam ply file
 node_feature_dim: 512
 gnn:
  jointgnn:
   version: "v2"
   pts_msg_method: MSG_FAN
   img_msg_method: MSG_MV_DIRECT
  MSG_FAN:
   aggr: 'max'
   dim_atten: 256
   num_layers: 2
   num_heads: 8
   drop_out: 0.3
   use_bn: false
  MSG_MV_DIRECT:
   aggr: max
   use_res: False
  MSG_MV_GAT:
   aggr: mean
  MSG_MV_SDPA:
   aggr: add
   num_heads: 8
  MSG_FAN:
   aggr: 'max'
   num_heads: 8
   use_bn: false
   attn_dropout: 0.5
data:
 load_cache: true
 img_desc_6_pts: false # return 6 extreme points 
 drop_img_edge: 4 # if is int and >0, select given number. if is float/double, select percentage between [1-x,1]
 use_precompute_img_feature: false
wandb:
 dry_run: false
 tags: ["ssg", "jointSSG",'orbslam','l20']
